export const initialSignUpState = {
  firstName: "",
  lastName: "",
  password: "",
};

const signUpReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default signUpReducer;
